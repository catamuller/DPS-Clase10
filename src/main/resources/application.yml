spring:
  application:
    name: Exchange
  profiles:
    active: sql
server:
  port: 8080
  servlet:
    context-path: /api    
logging:
  level:
    root: INFO
    edu.itba.class10: DEBUG
exchange-api:
  http-providers:
    - name: free-currency
      api-key: ${EXCHANGE_API_KEY}
      base-url: https://api.freecurrencyapi.com/v1/latest
http:
  client: "unirest"
  #client: "resttemplate"